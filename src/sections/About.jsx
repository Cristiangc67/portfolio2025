import { useGSAP } from "@gsap/react";
import gsap from "gsap";
import React, { useRef } from "react";

const About = () => {
  return (
    <>
      <section
        id="sobre-mi"
        className="min-h-dvh w-full  bg-amber-300 relative"
      >
        <div className="text-6xl z-10 absolute"> BSAS</div>
      </section>
    </>
  );
};

export default About;
